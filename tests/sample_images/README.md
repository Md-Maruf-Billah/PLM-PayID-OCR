Intentionally empty. Real customer slip scans must never be committed here
(see `.gitignore` and `docs/setup.md`). Test slips are generated
synthetically at test time -- see `tests/test_ocr_pipeline.py`.
