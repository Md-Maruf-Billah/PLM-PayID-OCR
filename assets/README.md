Drop `success.wav` and `failure.wav` here (paths referenced by
`config/config.json` -> `sounds`). If a file is missing, `src/sounds.py`
falls back to the terminal bell so scanning still gives audible feedback.
